# Remaining LatencyPilot 1.0 Source Completion Plan — Final Source Handoff

Status: **Repository-verifiable implementation complete; exact-final-HEAD Tests is the repository closure proof, while physical product closure remains**  
Reconciled: 2026-09-15

**Goal:** Preserve one concise authoritative handoff for the source-completion tranche that began after the internal GPU execution work. Historical RED/GREEN details remain in Git history and CI; this file now records what is actually complete, what remains blocked by physical prerequisites, and the next authorized action.

**Spec:** `docs/superpowers/specs/2026-09-14-product-1.0-source-completion-design.md`  
**Roadmap authority:** `ROADMAP.md`  
**Live evidence authority:** `PROJECT_STATUS.md`

## Invariants

- Work directly on `main`; preserve unrelated work.
- Hosted GitHub Actions remains **test-only**.
- Public protocol remains observation-only v6 (`GetStatus`, `CaptureKernelLatency`) and `MutationAvailable=false` until the documented physical gate sequence authorizes product mutation.
- No generic privileged shell/process/registry executor, generic tweak framework, second persistence engine, second IPC stack or second UI framework.
- Missing/ambiguous Windows evidence remains unavailable/inconclusive; never infer ports, queues, processors, MSI ownership, runtime placement or improvement from names or folklore.
- Default permanent-test target is 10; current durable suite is 16 under the owner-authorized maximum of 20 for materially safer subsystem separation / `<=1200` lines per test file.
- Source-complete never closes hardware, App/Service runtime, installer, signing, accessibility or physical mutation evidence.

## Completed repository-verifiable tranche

- [x] GPU bounded screening orchestration.
- [x] synchronized ETW + raw PresentMon evidence.
- [x] runtime GPU ISR-placement validity contract.
- [x] owner Gate A placement verification requires exact stored candidate state before/after a clean ETW interval plus direct GPU-driver ISR placement; ConfigMgr allocated-resource data is provenance only.
- [x] fixed eight-run `ABBA + BAAB` finalist confirmation.
- [x] explicit five-way verdict interpretation and verified Keep/Restore transitions.
- [x] interruption-aware GPU affinity mutation/recovery substrate.
- [x] exact documented USB hub/port route source with ambiguity handling.
- [x] bounded host-observable Raw Input timing capture and analysis.
- [x] xHCI DPC/ISR attribution/readiness contracts.
- [x] authoritative StandardCimv2 RSS inventory and conservative PnP correlation.
- [x] network DPC/ISR attribution and local RTT/jitter/loss/throughput/CPU benchmark interpretation.
- [x] versioned Competitive/Gaming, General and Audio-sensitive profiles.
- [x] Pareto trade-off policy with no weighted composite score.
- [x] retained-change discovery and fail-closed global Restore Baseline planning/execution for supported GPU changes.
- [x] recovery-aware install/upgrade/uninstall source.
- [x] deterministic payload/checksum/signing/release-provenance source.
- [x] redacted local diagnostics bundle source with no automatic upload.
- [x] `workload-stability-v1` shared optimizer eligibility, including early/late drift and isolated extreme-window rejection.
- [x] App candidate preparation uses the shared valid-baseline + stable-workload readiness boundary.
- [x] owner `plan-gpu-affinity` reconstructs the same workload activity evidence from evidence-v8 `runtimeWindows` and refuses planning unless `GpuOptimizationBaselineReadiness` passes.
- [x] the CriticalTests project builds `LatencyPilot.PhysicalValidation` as a non-reference project dependency, so exact-HEAD Tests cannot stay green while the owner Gate A tool fails to compile; no permanent test method was added for this compile gate.
- [x] App read-only inspector source surfaces exact USB input route/port evidence, RSS provider/PnP evidence and explicit on-demand five-second host timing.
- [x] `ROADMAP.md`, `PROJECT_STATUS.md`, `README.md`, `SYSTEM_DESIGN.md`, benchmark methodology and source-completion spec reconciled to current source semantics.

## Deliberately gated implementation

These are **not** ordinary source TODOs to bypass. They depend on physical evidence from the shared mutation substrate:

- [ ] mutation-specific typed product IPC — blocked until Gate A passes;
- [ ] user-facing GPU one-click mutation — blocked until Gate C/D;
- [ ] supported xHCI/controller-affinity mutation implementation — blocked until Gate A validates shared restart/rollback/recovery behavior physically;
- [ ] supported RSS/affinity mutation implementation — blocked until Gate A validates shared restart/rollback/recovery behavior physically;
- [ ] armed multi-subsystem automatic profile session — blocked until supported USB/NIC mutation paths exist and pass their own physical evidence requirements.

Do not implement these by weakening or skipping the gate sequence.

## Physical closure sequence

### Phase 2 read-only closure

- [ ] Controlled-idle valid five-window baseline on the closure revision.
- [ ] representative GPU/NIC/xHCI/USB/RSS inspector sanity.
- [ ] independent attribution plausibility where practical.
- [ ] App-close/Service-restart/stale-ETW cleanup and active-session rejection.
- [ ] visible UI ↔ JSON ↔ SHA ↔ exact source revision reconciliation.
- [ ] Light/Dark/High Contrast, narrow/text scaling, keyboard and UIA/screen-reader sanity.
- [ ] proof read-only validation performs zero unrelated mutation.

### Gate A — internal GPU substrate proof

- [ ] build/install/launch exact current `main` App + protected Service;
- [ ] journal starts clean with zero unresolved;
- [ ] controlled unresolved state survives Service restart/reclassification from actual machine state;
- [ ] exact-target restart / reboot-required behavior is physically verified;
- [ ] one candidate apply → stored verify → restart → runtime attributed ISR placement → exact rollback;
- [ ] one deliberate supported failure proves recovery;
- [ ] final exact original machine state + zero unresolved.

### Gate B/C/D

- [ ] Gate B: implement typed mutation-specific allowlisted IPC/authorization after Gate A.
- [ ] Gate C: physically validate the real App/client → Service mutation boundary and rollback.
- [ ] Gate D: arm the supported user-facing GPU workflow only after Gate C.

### Remaining supported domains and release

- [ ] implement + physically validate the supported USB/xHCI mutation after Gate A.
- [ ] implement + physically validate the supported NIC/RSS mutation after Gate A.
- [ ] validate bounded multi-subsystem profile execution + Restore Baseline.
- [ ] owner-local App/Service Release build + launch smoke.
- [ ] real Authenticode signing/verification/RFC3161 timestamp evidence.
- [ ] clean-machine installer + portable validation.
- [ ] safe and blocked upgrade/uninstall cases.
- [ ] reboot/crash/recovery validation.
- [ ] accessibility and representative supported-hardware audit.
- [ ] final 1.0 source/package/evidence/tag reconciliation.

## Current next action

1. Require a successful **Tests** workflow for the exact current documentation/source HEAD before using it as closure evidence.
2. Confirm the public v6/no-mutation boundary remains unchanged on that same HEAD.
3. Once that repository gate is green, stop repository-only source work at the physical boundary rather than inventing evidence.
4. On the owner Windows 11 machine, execute the Phase 2 closure and Gate A runbooks on that exact revision.
5. Only after Gate A evidence exists, resume source implementation at Gate B and the physically authorized USB/NIC mutation paths.

## Completion rule

This source tranche may be called **repository/source-complete** only when the exact HEAD being handed to physical validation has a green test-only **Tests** workflow. It may **not** be called true product `1.0` or `100% complete` until every remaining physical gate above is satisfied with exact recorded evidence.
